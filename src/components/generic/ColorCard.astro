---
import { pallete } from "./colors";
import CustomCard from "./CustomCard.astro";

interface Props {
  color?: keyof typeof pallete;
  full?: boolean;
  space?: boolean;
}

const { color = "base", full, space } = Astro.props;

const selectedColor = pallete[color];
---

<CustomCard color={selectedColor} full={full} space={space}>
  <slot />
</CustomCard>
